(function($){var gocommerceWooCommerce={installExtension:function(e){e.preventDefault();if($(this).is('[disabled=disabled]')){return}
		var $parentRow=$(this).closest('.gocommerce-wc-addon');$(this).replaceWith('<div class="js-installing">'+gocommerceWooCommerceExtensions.installingMarkup+'</div>');$.post(ajaxurl,{'action':'install_woocommerce_extension','extensionSlug':$(this).data('slug'),'extensionName':$(this).data('name'),'downloadLink':$(this).data('download-link'),'_ajax_nonce':$(this).data('nonce'),},function(response){if(!response.success){console.error(response.data);$parentRow.find('.js-installing').replaceWith('<div class="error"><p>'+response.data.errorMessage+'</p></div>');return}
			$parentRow.find('.js-installing').replaceWith(response.data.actionLinks)})},activateExtension:function(e){e.preventDefault();if($(this).is('[disabled=disabled]')){return}
		var $parentRow=$(this).closest('.gocommerce-wc-addon');$(this).text(gocommerceWooCommerceExtensions.activatingText).attr('disabled','disabled');$.post(ajaxurl,{'action':'activate_woocommerce_extension','extensionSlug':$(this).data('slug'),},function(response){if(!response.success){console.error(response.data);$parentRow.find('.js-activate').replaceWith('<div class="error"><p>'+response.data.errorMessage+'</p></div>');return}
			$parentRow.find('.js-activate').replaceWith(response.data.activeMarkup)})},};$(document).on('click','.js-install-extension',gocommerceWooCommerce.installExtension);$(document).on('click','.js-activate',gocommerceWooCommerce.activateExtension)})(jQuery)
